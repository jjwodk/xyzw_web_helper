# 2026-04-02 工作记录

## 分析：阵容助手卡片（Unlimitedlineup.vue）的装备淬炼逻辑

**调查文件**：
- `src/components/cards/Unlimitedlineup.vue`（3127行，核心组件）
- `src/components/GameStatus.vue`（工具Tab挂载位置）
- `src/utils/xyzwWebSocket.js`（WebSocket命令注册）

**关键结论**：
- 装备淬炼在存/换阵容流程中**完全不涉及**，阵容保存只存英雄、等级、鱼灵、鱼珠、科技、武器
- 装备淬炼仅作为**只读查看**功能存在：点击英雄头像 → `showHeroRefineModal()` → 弹窗展示淬炼孔位数据
- WebSocket已注册三个淬炼命令（equipment_quench/confirm/updatequenchlock），但组件内均未调用，只调用了展示弹窗
- 存阵容保存字段：position, heroId, level, attachmentUid, fishId, pearlId, skillId, slotMap, legionResearch, weaponId
- 换阵容步骤：① 处理attachmentUid归属冲突 → ② 下阵旧英雄 → ③ 上阵新英雄 → ④ 调整等级 → ⑤ 换鱼灵/鱼珠技能 → ⑥ 同步科技 → ⑦ 切换武器
