# 2026-04-14 日志

## MainActivity.java 编译错误修复
- 错误：`onResume() in MainActivity cannot override onResume() in BridgeActivity — attempting to assign weaker access privileges; was public`
- 原因：`onResume()` 用了 `protected`，但父类 `BridgeActivity` 是 `public`，Java 禁止子类降低可见性
- 修复：将第 23 行 `protected void onResume()` 改为 `public void onResume()`
- 已 commit (`1269ee7`) 并 push 到 GitHub main 分支，CI 重新触发
