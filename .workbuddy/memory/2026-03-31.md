# 2026-03-31

## 本地部署项目

- 项目：xyzw-token-manager（XYZW游戏Token管理器）
- 操作：将 vite.config.js 中 server.port 从 3000 改为 10034
- 执行了 pnpm install 安装依赖（572个包）
- 使用 pnpm run dev 启动开发服务器
- 访问地址：http://localhost:10034
- 创建了 启动.bat 快捷启动脚本（自动安装依赖 + 启动开发服务器）

## 端口回滚问题

- 用户反馈：从 10035 换到 10034 后数据全部丢失
- 原因：浏览器的 localStorage 和 IndexedDB 按端口隔离，不同端口 = 不同源
- 已将端口改回 10035，数据恢复
- 更新了 启动.bat 中的端口提示信息

## Git 更新项目至 v2.1.2

- 原始仓库：https://github.com/w1249178256/xyzw_web_helper.git
- 操作：git init → remote add → fetch → commit 本地版本 → pull --allow-unrelated-histories
- 唯一冲突：vite.config.js 端口（保留 10035）
- 代理设置：git config --global http.proxy http://127.0.0.1:7892（Clash，端口7892）
- 更新后 pnpm install 无新依赖，Already up to date
